\chapter{Probas}
\label{ch:probas}

A verificación da funcionalidade e da corrección global do sistema aborda dúas
dimensións complementarias: as \textbf{probas automatizadas} do backend, que garanten a
corrección técnica da lóxica e da API, e a \textbf{validación funcional} de extremo a
extremo da aplicación, que comproba que o produto se comporta como espera o usuario.

\section{Estratexia de probas}

A estratexia segue o modelo da pirámide de probas, con tres niveis:

\begin{itemize}
  \item \textbf{Probas unitarias}: illan a lóxica de negocio de cada servizo ou módulo,
        substituíndo as dependencias por dobres de proba (\textit{mocks}). Aplícanse tanto
        no backend (JUnit~5) como no frontend (Jest).
  \item \textbf{Probas de integración (E2E)}: exercitan a API completa contra unha base de
        datos e un almacén de obxectos reais, levantados en contedores efémeros.
  \item \textbf{Validación de sistema}: comprobación manual dos fluxos de usuario completos
        sobre a aplicación en execución.
\end{itemize}

\section{Probas automatizadas do backend}

O backend conta cunha batería de \textbf{410 probas automatizadas} repartidas en 30 clases,
que se executan co \textit{framework} JUnit~5. A Táboa~\ref{tab:probas} resume a súa
distribución.

\begin{table}[H]
  \centering
  \begin{tabularx}{\textwidth}{@{}lcX@{}}
    \toprule
    \textbf{Tipo} & \textbf{Clases} & \textbf{Descrición} \\
    \midrule
    Unitarias de servizos   & 11 & Lóxica de negocio illada con Mockito. \\
    Integración (E2E)        & 12 & API completa con base de datos e almacén reais (Testcontainers). \\
    Modelo                   & 4  & Lóxica propia das entidades de dominio. \\
    Tarefas programadas      & 3  & Comportamento dos \textit{schedulers} de limpeza. \\
    \midrule
    \textbf{Total}           & \textbf{30} & \textbf{410 probas} \\
    \bottomrule
  \end{tabularx}
  \caption{Distribución das probas automatizadas do backend.}
  \label{tab:probas}
\end{table}

As probas de integración empregan \textbf{Testcontainers}~\cite{testcontainers}, que levanta
contedores efémeros de PostgreSQL e MinIO para cada execución, de xeito que as probas se
realizan contra servizos reais e non contra simulacións. Unha clase base común
(\texttt{BaseE2ETest}) orquestra o arrinque dos contedores e a aplicación execútase cun
perfil de proba que recrea o esquema da base de datos en cada execución. Deste xeito
verifícanse os fluxos completos da API: autenticación, xestión de viaxes e membros, gastos
e liquidacións, eventos, documentos, chat e asistente de IA.

A cobertura de código mídese con \textbf{JaCoCo}~\cite{jacoco}. As métricas exclúen o
código xerado automaticamente a partir da especificación OpenAPI (os \textit{DTO} e as
interfaces \texttt{*Api}), seguindo a práctica habitual de non contabilizar o código que
non se escribe a man: incluílo só distorsionaría as cifras, xa que os seus métodos
\texttt{equals}, \texttt{hashCode} e construtores nunca se exercitan. A
Táboa~\ref{tab:cobertura} resume os resultados obtidos sobre o código propio na execución
completa da batería.

\begin{table}[H]
  \centering
  \begin{tabularx}{\textwidth}{@{}Xcc@{}}
    \toprule
    \textbf{Métrica} & \textbf{Cobertura} & \textbf{} \\
    \midrule
    Liñas       & 92,6\,\% & \\
    Instrucións & 91,4\,\% & \\
    Métodos     & 92,9\,\% & \\
    Clases      & 96,7\,\% & \\
    Ramas       & 85,0\,\% & \\
    \bottomrule
  \end{tabularx}
  \caption{Cobertura de código do backend (JaCoCo).}
  \label{tab:cobertura}
\end{table}

A cobertura concéntrase na capa de servizos e nos fluxos principais da API, que é onde
reside a lóxica de negocio, e supera o 90\,\% de liñas. As cifras lixeiramente máis baixas
en ramas corresponden, en boa medida, a ramas defensivas e de manexo de erros pouco
frecuentes. Ademais, a construción e a execución das probas intégranse nun fluxo de
integración continua (GitHub Actions), e o código sométese a análise estática.

\section{Probas automatizadas do frontend}

O frontend conta cunha batería de \textbf{267 probas automatizadas} en 20 suites, executadas
con \textbf{Jest} mediante o preset oficial de Expo (\texttt{jest-expo}). Para as probas de
compoñentes e \textit{hooks} utilizouse \texttt{@testing-library/react-native}, que promove
probas centradas no comportamento observable polo usuario en lugar de detalles de
implementación internos.

As probas organizáronse segundo tres orientacións complementarias:

\begin{itemize}
  \item \textbf{Probas de contrato e lóxica pura}: cubren módulos sen dependencias de React
        Native, como a clase de erros (\texttt{AppError}), as claves de caché de React Query
        (\texttt{queryKeys}), as funcións auxiliares de documentos
        (\texttt{documentCards.helpers}) e as funcións de persistencia de sesión
        (\texttt{lastTrip}). Son especialmente robustas fronte a cambios de interface: só
        fallan se muda a lóxica de negocio.

  \item \textbf{Probas de comportamento e interacción}: cubren o \textit{hook} de
        accesibilidade (\texttt{useReducedMotion}), a función de subida de ficheiros
        (\texttt{upload}), o compoñente de texto temático (\texttt{ThemedText}) e o control
        de segmentos (\texttt{SegmentedControl}). Verifican o comportamento observable: que
        ao premer un segmento se invoca o \textit{callback} co valor correcto, ou que un
        código de estado HTTP de erro rexeita a promesa.

  \item \textbf{Probas de \textit{hooks} de API e contextos}: cubren os módulos de lóxica
        de acceso a datos e xestión de estado. As de \texttt{friends.tsx} verifican que cada
        \textit{hook} invoca o punto de acceso REST correcto e que os erros HTTP se propagan
        como \texttt{AppError}; tamén comproba que \texttt{useRemoveFriend} e
        \texttt{useInviteFriendToTrip} invalidan as claves de caché correspondentes. As de
        \texttt{aiChat.tsx} verifican a construción da URL (incluída a codificación do
        parámetro \texttt{before}), a entrega de tokens mediante SSE e o mecanismo de
        cancelación. As de \texttt{dataSaver.tsx} e \texttt{preferences.tsx} comproba que os
        provedores len o valor almacenado e persisten os cambios, e que os valores
        descoñecidos se normalizan a un valor por defecto. Os módulos \texttt{expenses.tsx},
        \texttt{invitations.tsx}, \texttt{events.tsx}, \texttt{trips.tsx},
        \texttt{documents.tsx} e \texttt{users.tsx} achegan probas para todas as súas
        funcións exportadas: validación dos parámetros enviados á API, procesamento de
        respostas (incluíndo erros HTTP e valores nulos), e, nos módulos de documentos e
        avatares, o fluxo de carga en dúas fases (inicialización, subida externa e
        confirmación). As de \texttt{groupChat.tsx} cobren o ciclo de mensaxes en tempo real:
        carga do historial, eventos SSE (incluíndo deduplicación e supresión de mensaxes
        propias), envío e o contador de non lidos. As de \texttt{auth.tsx} exercitan o ciclo
        de vida completo da autenticación: restauración de sesión ao arrancar, inicio e peche
        de sesión, rexistro, renovación automática de token con deduplicación de peticións
        concorrentes e as funcións auxiliares de actualización do perfil.
\end{itemize}

A cobertura mídese con Jest sobre os \textbf{módulos directamente exercitados} polas
probas. Por defecto, Jest só contabiliza os ficheiros que se importan durante a execución,
de xeito que as pantallas da aplicación (que non forman parte desta batería) non aparecen
na análise. Polo tanto, as cifras da Táboa~\ref{tab:cobertura-frontend} reflicten a
calidade da cobertura dentro dos módulos elixidos, non a cobertura global do frontend.

\begin{table}[H]
  \centering
  \begin{tabularx}{\textwidth}{@{}Xcccc@{}}
    \toprule
    \textbf{Módulo} & \textbf{Instrucións} & \textbf{Ramas} & \textbf{Funcións} & \textbf{Liñas} \\
    \midrule
    \texttt{events.tsx}               & 100\,\% & 100\,\% & 100\,\% & 100\,\% \\
    \texttt{friends.tsx}              & 100\,\% & 100\,\% & 100\,\% & 100\,\% \\
    \texttt{lastTrip.ts}              & 100\,\% & 100\,\% & 100\,\% & 100\,\% \\
    \texttt{preferences.tsx}          & 100\,\% & 100\,\% & 100\,\% & 100\,\% \\
    \texttt{ThemedText.tsx}           & 100\,\% &  86\,\% & 100\,\% & 100\,\% \\
    \texttt{upload.ts}                & 100\,\% & 100\,\% & 100\,\% & 100\,\% \\
    \texttt{useReducedMotion.ts}      & 100\,\% &  50\,\% & 100\,\% & 100\,\% \\
    \texttt{users.tsx}                &  98\,\% &  91\,\% & 100\,\% & 100\,\% \\
    \texttt{documents.tsx}            &  96\,\% &  88\,\% & 100\,\% & 100\,\% \\
    \texttt{dataSaver.tsx}            &  94\,\% & 100\,\% &  80\,\% & 100\,\% \\
    \texttt{expenses.tsx}             &  92\,\% &  92\,\% &  92\,\% &  93\,\% \\
    \texttt{auth.tsx}                 &  92\,\% &  82\,\% & 100\,\% &  92\,\% \\
    \texttt{documentCards.helpers.ts} &  92\,\% &  80\,\% & 100\,\% &  91\,\% \\
    \texttt{groupChat.tsx}            &  92\,\% &  73\,\% &  77\,\% &  94\,\% \\
    \texttt{queryKeys.ts}             &  92\,\% & 100\,\% &  88\,\% &  92\,\% \\
    \texttt{trips.tsx}                &  84\,\% &  83\,\% &  75\,\% &  85\,\% \\
    \texttt{SegmentedControl.tsx}     &  80\,\% &  41\,\% &  83\,\% &  77\,\% \\
    \texttt{invitations.tsx}          &  76\,\% &  75\,\% &  73\,\% &  77\,\% \\
    \texttt{aiChat.tsx}               &  45\,\% &  30\,\% &  73\,\% &  44\,\% \\
    \texttt{AppError.ts}              &  22\,\% &  25\,\% &  50\,\% &  22\,\% \\
    \midrule
    \textbf{Media}                    &  93\,\% &  80\,\% &  90\,\% &  88\,\% \\
    \bottomrule
  \end{tabularx}
  \caption{Cobertura de código do frontend (Jest) sobre os módulos probados.}
  \label{tab:cobertura-frontend}
\end{table}

A cobertura baixa de \texttt{AppError.ts} (22\,\% de instrucións) débese a que o ficheiro
contén unha función privada non exportada (\texttt{mapHttpStatusToErrorCode}) que as probas
non poden invocar directamente; a API pública do módulo (a clase e a enumeración) está
completamente cuberta. As cifras de \texttt{SegmentedControl.tsx} reflicten que o código de
animación e o manejador de \textit{layout} non se activan no entorno de proba, onde o
compoñente non realiza medicións reais de anchura. A rama non cuberta de
\texttt{useReducedMotion.ts} correspóndese co caso de carreira no que o compoñente se
desmonta xusto antes de que a promesa de \texttt{AccessibilityInfo} resolva, o que resulta
difícil de reproducir de forma determinista nun test. A cobertura baixa de
\texttt{aiChat.tsx} (45\,\% de instrucións) débese á clase \texttt{OneShotEventSource}, que
estende a biblioteca \texttt{react-native-sse}: dado que esta biblioteca está substituída por
un \textit{mock} nas probas, o código da clase nunca se executa e Jest contabiliza todas as
súas liñas como non cubertas; os \textit{hooks} exportados do módulo, que é o que se está a
verificar, están completamente cubertos. A función \texttt{setDataSaver} de
\texttt{dataSaver.tsx} non está cuberta ao 100\,\% en funcións (80\,\%) porque a variante
interna de inicialización diferida non é exercitada directamente polas probas. A cobertura
parcial de \texttt{expenses.tsx}, \texttt{invitations.tsx} e \texttt{trips.tsx} débese a
\textit{hooks} internos non exportados (\texttt{usePastSettlementsQuery},
\texttt{useResolveInvitationMutation} e \texttt{useLeaveTripMutation}/\texttt{useTripQuery},
respectivamente) que non se poden invocar directamente desde as probas. As liñas non
cubertas de \texttt{auth.tsx} (92\,\% de instrucións) corresponden a ramas defensivas dentro
das funcións privadas \texttt{loginRequest} e \texttt{registerRequest} e á garda de
\textit{parsing} do JWT. As de \texttt{groupChat.tsx} (92\,\% de instrucións) corresponden
ao manejador de erros SSE e algunhas ramas de estado de chat que non se activan nas
combinacións de proba elixidas.

\section{Validación funcional do sistema}

Para verificar o comportamento de extremo a extremo executáronse manualmente sobre a
aplicación os escenarios de uso descritos na Táboa~\ref{tab:escenarios}, que cobren a
totalidade dos requisitos funcionais. Cada escenario comprende a secuencia completa de
accións do usuario e a comprobación do resultado esperado.

\begin{table}[H]
  \centering
  \begin{tabularx}{\textwidth}{@{}llX@{}}
    \toprule
    \textbf{ID} & \textbf{RF} & \textbf{Escenario verificado} \\
    \midrule
    CP01 & RF01, RF02      & Rexistro, inicio e peche de sesión, e renovación de token. \\
    CP02 & RF03--RF05      & Crear viaxe, convidar e aceptar membros (e por código QR). \\
    CP03 & RF06, RF07      & Rexistrar gastos, consultar balances e liquidar débedas. \\
    CP04 & RF08            & Crear, consultar e eliminar eventos do calendario. \\
    CP05 & RF09            & Subir, previsualizar e eliminar documentos. \\
    CP06 & RF10            & Enviar e recibir mensaxes no chat de grupo en tempo real. \\
    CP07 & RF11            & Conversar co asistente de IA e recuperar o historial. \\
    CP08 & RF12, RF13      & Xestionar amizades e convidar amigos a unha viaxe. \\
    CP09 & RF14, RF15      & Editar o perfil e eliminar a conta con confirmación. \\
    \bottomrule
  \end{tabularx}
  \caption{Escenarios de validación funcional e a súa trazabilidade cos requisitos.}
  \label{tab:escenarios}
\end{table}

\section{Avaliación de usabilidade con usuarios reais}

Para complementar a validación técnica cunha perspectiva centrada na experiencia de uso,
realizouse unha avaliación de usabilidade mediante o \textbf{protocolo de pensar en voz
alta} (\textit{think-aloud})~\cite{nielsen1994usability}. Nesta técnica, os participantes
verbalizan os seus pensamentos mentres interactúan libremente co sistema, o que permite ao
avaliador identificar tanto obstáculos de navegación como erros de comprensión que os
escenarios de validación estruturados non detectan.

As sesións realizáronse de forma moderada con \textbf{15 participantes} en total. Non se
definiron tarefas pechadas: pedíuselle a cada participante que explorara a aplicación como
se a fose a usar no seu día a día, interviñendo o avaliador só para solicitar que
verbalizasen o que estaban a pensar. Anotábanse os problemas observados e, ao rematar a
sesión, recollíanse as impresións xerais do participante.

Os problemas identificados priorizábanse segundo dous criterios: (1) \textbf{frecuencia},
cando varios participantes atopaban o mesmo obstáculo; e (2) \textbf{severidade}, cando un
único participante atopaba un bloqueo evidente que impedía completar un fluxo. Este dobre
criterio é coherente coa práctica habitual da enxeñaría de usabilidade para distinguir
incidencias menores de problemas críticos~\cite{nielsen1994usability}. O proceso continuou
de forma iterativa, aplicando correccións entre sesións, ata alcanzar a
\textbf{saturación}: o punto no que as sesións deixaron de producir problemas novos
significativos.

Entre os aspectos corrixidos como resultado desta avaliación destacan:

\begin{itemize}
  \item Acceso directo aos documentos do día seleccionado desde o calendario, que
        anteriormente requiría navegar a unha pantalla separada.
  \item Eliminación do diálogo de confirmación para volver ao menú principal, percibido
        como un paso innecesario que interrompía o fluxo de navegación.
  \item Ordenación das viaxes por data de creación, xa que os participantes esperaban
        atopar a viaxe máis recente en primeiro lugar.
  \item Incorporación dun mapa interactivo no modal de creación de eventos, para facilitar
        a selección da localización sen abandonar a pantalla.
  \item Engadido de categorías aos gastos, que varios participantes botaron en falta para
        organizar e filtrar as partidas dunha viaxe.
\end{itemize}

Ao final das sesións, os participantes completaban todos os fluxos principais sen atopar
obstáculos relevantes e valoraban positivamente a claridade da navegación e a consistencia
visual da aplicación.

\section{Resultados acadados}

A avaliación de usabilidade con 15 participantes mediante o protocolo de pensar en voz
alta alcanzou a saturación sen detectar problemas críticos pendentes, confirmando que os
fluxos principais son navegables e comprensibles para os usuarios obxectivo.

A totalidade das \textbf{677 probas automatizadas pasan correctamente}: 410 do backend
(JUnit~5) e 267 do frontend (Jest). Os nove escenarios de validación funcional completáronse
co resultado esperado, polo que se verifica o cumprimento de todos os requisitos funcionais.
A cobertura do backend supera o 90\,\% de liñas sobre o código propio; no frontend, os
20 módulos directamente exercitados polas probas acadan de media un 88\,\% de liñas e un
90\,\% de funcións, aínda que estes valores non reflicten a cobertura global do frontend.
